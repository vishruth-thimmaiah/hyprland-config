general {
    lock_cmd = hyprlock
    unlock_cmd =
    before_sleep_cmd = hyprlock
    after_sleep_cmd =
    ignore_dbus_inhibit = false
}

listener {
    timeout = 25
    on-timeout = if [[ $(pgrep hyprlock) ]]; then systemctl suspend; fi
    on-resume =
}


listener {
    timeout = 300
    on-timeout = hyprlock
    on-resume =
}


listener {
    timeout = 330
    on-timeout = systemctl suspend
    on-resume =
}

# vim: filetype=hyprlang: commentstring=#\ %s
